---
interface Props {
  label?: string;
}
const { label } = Astro.props;
---

<div class="mb-[18px] last:mb-0">
  {
    label ? (
      <div class="mb-2.5 text-[11px] font-semibold uppercase tracking-[0.06em] text-[#333]">{label}</div>
    ) : null
  }
  <slot />
</div>
